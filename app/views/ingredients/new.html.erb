<h1>Add an ingredient</h1>

<%= form_for @ingredient do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :quantity %>
  <%= f.text_field :quantity %>
  <%= f.label :recipe %>
  <%= f.collection_select :recipes, Recipe.all, :id, :title %>
  <%= f.submit %>
<% end %>
